# описание функций в голосовом помщнике

'''def history():
    z = {}
    arr = {}
    arr1 = {}
    arr2 = {}
    arr3 = {}
    arr4 = {}
    file = open('history.txt' , 'r', encoding = 'UTF-8')
    k = file.readlines()
    for i in range(len(k)):
        line = str(k[i].replace('\n','').strip())
        arr.append(line)
    file.close()
    for i in range(len(arr)):
        x = arr[i]
        if x in z:
            z[x] += 1
        if not(x in z):
            b = {x : 1}
            z.update(b)
        if not(x in arr2):
            arr2.append(x)
    for i in arr2:
        arr3.append(z[i])
    for i in range(1, len(arr3)+1):
        arr4.append(str(i)+') ')
    list = DataFrame({               pd.DataFreme надо разобраться
        'command' : arr2,
        'count' : arr3
    }, index = arr4)
    list.index.name = '№'
    print(list) '''